var schedule = require('node-schedule');

var Job = function () {

    this.startGameEndJob = function () {
       app.use(express.static(__dirname + "../../docs/html"));
        };
    };

    this.startJobs = function () {
        this.startGameEndJob();
         };
};

module.exports = Job; 
